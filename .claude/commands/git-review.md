Review all uncommitted changes and provide feedback:

1. Run `git diff` to see unstaged changes and `git diff --cached` to see staged changes
2. For each changed file, analyze:
   - Does the change follow project conventions from CLAUDE.md?
   - Are there any bugs, typos, or missing type hints?
   - Are there any security concerns?
3. Suggest improvements if needed
4. If everything looks good, say so and suggest a commit message

Keep feedback concise and actionable.
