Show a concise summary of the current git state:

1. Run `git status` to show working tree status
2. Run `git log --oneline -10` to show recent commits
3. Run `git branch -a` to show all branches
4. Run `git diff --stat` to show changed files summary

Present the results in a clean, organized format.
